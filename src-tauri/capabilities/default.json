{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["settings", "checker", "attendance"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "notification:default",
    "dialog:default"
  ]
}
