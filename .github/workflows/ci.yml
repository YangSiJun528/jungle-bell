name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft, labeled, unlabeled, edited]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  scope:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      mode: ${{ steps.scope.outputs.mode }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Select CI scope from the label and complete PR diff
        id: scope
        run: python3 scripts/ci_scope.py

  hygiene:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Set up prek
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.9.5"
          install_args: prek
      - run: prek validate-config prek.toml
      - run: prek run --all-files --group hygiene --show-diff-on-failure
      - name: Test CI scope selection
        run: python3 -B -m unittest discover -s scripts -p 'test_ci_scope.py' -v

  web:
    needs: scope
    if: needs.scope.outputs.mode == 'full'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.9.5"
          install_args: node
      - name: Cache npm downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - run: npm ci
        working-directory: frontend
      - run: npm run verify:web
        working-directory: frontend

  server:
    needs: scope
    if: needs.scope.outputs.mode == 'full'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Set up Java
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.9.5"
          install_args: java
      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
      - run: chmod +x server/gradlew
      - name: Run unit and PostgreSQL integration tests and build jars
        run: ./gradlew --no-daemon check :api:bootJar :worker:bootJar
        working-directory: server
      - name: Validate production Compose
        working-directory: server
        env:
          CLOUDFLARE_TUNNEL_TOKEN: ci-placeholder
        run: |
          if grep -Eq '\$\{[A-Z0-9_]+:-' deploy/compose.production.yml; then
            echo 'Production Compose must not define fallback values.' >&2
            exit 1
          fi
          docker compose --env-file deploy/.env.production.example -f deploy/compose.production.yml --profile quick-tunnel config --quiet
          if LAUNDRY_SOURCE_URL= docker compose --env-file deploy/.env.production.example -f deploy/compose.production.yml config --quiet >/dev/null 2>&1; then
            echo 'Production Compose accepted an empty LAUNDRY_SOURCE_URL.' >&2
            exit 1
          fi
      - name: Build API image
        run: docker build --target api-runtime --file server/Dockerfile .
      - name: Build Worker image
        run: docker build --target worker-runtime --file server/Dockerfile .

  desktop:
    needs: scope
    if: needs.scope.outputs.mode == 'full'
    name: desktop (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    env:
      JUNGLE_BELL_DATA_API_URL: https://jungle-bell.sijun-yang.com
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Set up Node.js and Rust
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.9.5"
          install_args: node rust
          cache: false
      - name: Ensure Rust components
        run: rustup component add --toolchain stable rustfmt clippy
      - name: Cache npm downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: desktop -> target
          cache-on-failure: false
      - run: npm ci
        working-directory: frontend
      - run: npm run verify:desktop
        working-directory: frontend

  required:
    name: required
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    needs: [scope, hygiene, web, server, desktop]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Require all checks selected for this change
        env:
          SCOPE_RESULT: ${{ needs.scope.result }}
          CI_MODE: ${{ needs.scope.outputs.mode }}
          HYGIENE_RESULT: ${{ needs.hygiene.result }}
          WEB_RESULT: ${{ needs.web.result }}
          SERVER_RESULT: ${{ needs.server.result }}
          DESKTOP_RESULT: ${{ needs.desktop.result }}
        run: |
          test "$SCOPE_RESULT" = "success"
          test "$HYGIENE_RESULT" = "success"
          case "$CI_MODE" in
            full) EXPECTED_RESULT=success ;;
            docs) EXPECTED_RESULT=skipped ;;
            *) echo "Unknown CI mode: $CI_MODE" >&2; exit 1 ;;
          esac
          test "$WEB_RESULT" = "$EXPECTED_RESULT"
          test "$SERVER_RESULT" = "$EXPECTED_RESULT"
          test "$DESKTOP_RESULT" = "$EXPECTED_RESULT"
          if [ "$CI_MODE" = "docs" ]; then
            echo 'Documentation-only PR: hygiene passed; web, server and desktop checks were intentionally skipped.' >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'Full CI: hygiene, web, server and desktop checks passed.' >> "$GITHUB_STEP_SUMMARY"
          fi
