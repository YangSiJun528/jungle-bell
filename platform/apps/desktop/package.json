{
  "name": "@jungle-bell/desktop",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "build": "tauri build --config src-tauri/tauri.conf.json",
    "dev": "tauri dev --config src-tauri/tauri.conf.json",
    "lint": "cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "release-check": "cross-env JB_APP_ORIGIN=https://verify.invalid JB_API_ORIGIN=https://verify.invalid cargo check --release --manifest-path src-tauri/Cargo.toml",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml && node --test src-tauri/tests/lms_collector.test.mjs",
    "typecheck": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "cross-env": "^10.1.0"
  }
}
