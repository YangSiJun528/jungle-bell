{
  "name": "jungle-bell-desktop",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "workspaces": [
    "site"
  ],
  "scripts": {
    "dev": "vite",
    "build": "npm run build:app && npm run build:site && npm run assemble:site",
    "build:app": "tsc --noEmit && vite build",
    "build:site": "npm run build --workspace @jungle-bell/site",
    "assemble:site": "node site/scripts/assemble.mjs",
    "test": "vitest run",
    "test:site": "npm test --workspace @jungle-bell/site",
    "check:site": "npm run check --workspace @jungle-bell/site",
    "typecheck": "tsc --noEmit",
    "verify:web": "npm run check:site && npm run build && npm test && npm run test:site",
    "verify:server": "npm --prefix server run check && npm --prefix server run build",
    "verify:desktop": "npm run build:app && cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "verify": "npm run verify:web && npm run verify:server && npm run verify:desktop",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "alpinejs": "^3.15.12",
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2.11.4",
    "@types/alpinejs": "^3.13.11",
    "@types/node": "^26.1.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
